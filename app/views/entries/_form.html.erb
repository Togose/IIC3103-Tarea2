
<div style="width:650px; margin-top:100px; margin-left: auto; margin-right: auto">
  <%= bootstrap_form_for(@entry) do |f| %>

  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@entry.errors.count, "error") %> prohibited
        this entry from being saved:
      </h2>
      <ul>
        <% @entry.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <ul>
    <% @entry.errors.full_messages.each do |error| %>
      <li><%= error %></li>
    <% end %>
    </ul>
    <p>
      <%= f.text_field :title %>
    </p>
    <p>
      <%= f.text_area :subhead %>
    </p>
    <p>
      <%= f.text_area :body %>
    </p>
    <p>
      <%= f.submit %>
    </p>
  <% end %>
  <p>
  <%= button_to 'Volver', entries_path, :method => "get",
    class: "btn btn-primary" %>
  </p>
</div>
